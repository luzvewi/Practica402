class Coche(
     ruedas: Int,
     motor: String,
     numAsientos: Int,
     color: String,
     modelo: String,
     nombre: String // Nuevo campo para el nombre del vehículo
) : Vehiculo(ruedas, motor, numAsientos, color, modelo, nombre)